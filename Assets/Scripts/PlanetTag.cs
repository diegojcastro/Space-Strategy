using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlanetTag : MonoBehaviour
{
    public Color32 PLAYER = new Color32(0, 204, 0, 255);
    public Color32 ENEMY = new Color32(204, 0, 0, 255);
    public Color32 NEUTRAL = new Color32(204, 204, 0, 255);


    // Start is called before the first frame update
    void Start()
    {
        
    }

}
